
        from @std/app import start, print, exit

        type box<V> {
          set: bool,
          val: V
        }

        on start fn {
          let stringBox = new box<string> {
            set: true,
            val: 'str'
          };
          stringBox.val = 8;

          emit exit 0;
        }
      
