
        from @std/app import start, print, exit

        fn unreachable() {
          return 'blah';
          print('unreachable!');
        }

        on start {
          unreachable();
          emit exit 0;
        }
      
